# Trend Radar 專案故事

## 遇到什麼問題

每天追熱門話題時，資訊通常散在不同地方：搜尋趨勢、新聞網站、社群討論與即時推播各自分散。單次看到一個熱門關鍵字並不難，真正麻煩的是判斷它是剛爆紅、持續升溫，還是只短暫出現一次。

我想要一個集中式工具，能自動整理熱門關鍵字，保存歷史紀錄，並在異常升溫時主動通知，而不是每天手動打開多個網站查資料。

## 為什麼想做這個

Trend Radar 是以個人需求出發的小型資料產品。它不是一般 CRUD 系統，而是把 ETL、資料保存、排程、規則判斷、通知與 Dashboard 串在一起，形成一條完整的資料流程。

這個專案也能展示與 IT / Data / Backend 職缺相關的能力：Python 開發、SQL schema 設計、外部 API/RSS 整合、排程自動化、Docker 部署、測試，以及把資料轉成可查詢的產品介面。

## MVP 做了哪些取捨

MVP 只接 Google Trends Taiwan，因為第一版重點是驗證「定時抓取、保存、偵測、通知」這條核心流程，而不是處理多來源資料融合。

通知只做 Discord Webhook，不做 LINE、Telegram、Email 或 Discord bot 互動。Webhook 足以完成主動推播，也能降低憑證與互動事件處理的複雜度。

爆量偵測使用規則法，不做機器學習或 NLP。第一版只需要判斷排名突然上升、首次進入高排名或短時間維持高排名，規則法更容易解釋與測試。

Dashboard 不做登入。這是個人使用工具，第一版先把資料流與視覺化做完整，帳號與權限管理不是核心風險。

新聞補充只保存標題、來源、URL 與發布時間，不抓全文、不做摘要。這樣可以降低爬蟲風險，也讓資料模型維持簡單。

## 怎麼設計資料流程

資料流程從排程 worker 開始。系統定時抓取 Google Trends Taiwan RSS，將每次抓取視為一個 snapshot，並把每個關鍵字寫成 observation。關鍵字本身會存到 keyword 主檔，方便後續查詢歷史變化。

完成趨勢資料入庫後，系統會用每個 keyword 查 Google News RSS，保存少量相關新聞 metadata。接著執行 spike detection，根據排名變化與出現狀態產生 alert。若 alert 成立，系統會透過 Discord Webhook 推播。

Dashboard 透過 FastAPI 查詢資料庫，顯示最新熱門、最近 24 小時爆量警報、系統狀態與單一關鍵字歷史曲線。這樣資料抓取與畫面查詢分工清楚，排錯時也能從 job run 紀錄追蹤。

## 哪些功能故意先不做

多來源整合先不做。PTT、新聞熱門、社群資料都可能有價值，但每個來源都有不同資料結構與清洗成本，第一版先避免來源複雜度蓋過核心流程。

使用者系統先不做。這個工具目前以個人使用為主，登入、權限、多租戶都會增加大量非核心工作。

同義詞合併先不做。例如同一事件可能有不同關鍵字寫法，但 keyword normalization 與 entity resolution 需要額外規則，MVP 先保留原始關鍵字。

AI 摘要與全文分析先不做。這些功能可以提升展示效果，但也會增加成本、延遲與資料來源風險。第一版只提供新聞連結，讓使用者自己點進來源確認。

免費 serverless 排程先不做。GitHub Actions 或 serverless cron 可以作為後續展示，但目前先以本地 Docker / uvicorn 長時間運行為主，保留資料庫與 Dashboard 的完整體驗。
